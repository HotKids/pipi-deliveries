<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_express_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:theme="@style/PipiWidgetTheme">

    <ImageView
        android:id="@+id/widget_express_brand_gradient"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_express_gradient_surface"
        android:clipToOutline="true"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/widget_express_brand_gradient_mask" />

    <LinearLayout
        android:id="@+id/widget_express_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="14dp"
        android:paddingTop="14dp"
        android:paddingEnd="14dp"
        android:paddingBottom="14dp">

        <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="24dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <LinearLayout
            android:id="@+id/widget_express_header"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            tools:ignore="UseCompoundDrawables">

            <TextView
                android:id="@+id/widget_express_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false"
                android:textColor="?attr/colorOnSurface"
                android:textSize="16sp" />

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginStart="2dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_symbol_chevron_right"
                android:tint="?attr/colorOnSurfaceVariant"
                tools:ignore="UseAppTint" />
        </LinearLayout>

        <ImageView
            android:id="@+id/widget_express_search"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="@string/search_express_hint"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_search"
            android:tint="?attr/colorOnSurfaceVariant"
            tools:ignore="UseAppTint" />

    </LinearLayout>

        <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/widget_express_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:listSelector="@android:color/transparent"
            android:scrollbars="none" />

        </FrameLayout>
    </LinearLayout>

    <RelativeLayout
        android:id="@+id/widget_express_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_express_gradient_surface"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone">

        <ImageView
            android:id="@+id/widget_express_empty_gradient"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/widget_express_gradient_surface"
            android:clipToOutline="true"
            android:contentDescription="@null"
            android:scaleType="fitXY"
            android:src="@drawable/widget_express_brand_gradient_mask" />

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0.22"
            android:clipToOutline="true"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            android:src="@drawable/widget_express_empty_4x2_bg" />

        <LinearLayout
            android:id="@+id/widget_express_empty_header"
            android:layout_width="match_parent"
            android:layout_height="24dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="14dp"
            android:layout_marginEnd="8dp"
            android:layout_toStartOf="@id/widget_express_empty_search"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:text="@string/widget_express_empty_title"
                android:textColor="@color/widget_empty_title"
                android:textSize="14dp"
                tools:ignore="SpUsage" />

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginStart="2dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_symbol_chevron_right"
                android:tint="@color/widget_empty_text"
                tools:ignore="UseAppTint" />
        </LinearLayout>

        <ImageView
            android:id="@+id/widget_express_empty_search"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="14dp"
            android:layout_marginEnd="16dp"
            android:contentDescription="@string/search_express_hint"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_search"
            android:tint="?attr/colorPrimary"
            tools:ignore="UseAppTint" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="84dp"
            android:layout_alignParentBottom="true"
            android:layout_marginStart="-6dp"
            android:layout_marginEnd="-6dp"
            android:layout_marginBottom="-10dp"
            android:background="@drawable/widget_express_empty_4x2_text_bg" />

        <ImageView
            android:id="@+id/widget_express_empty_box"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_alignParentBottom="true"
            android:layout_marginStart="20dp"
            android:layout_marginBottom="12dp"
            android:contentDescription="@null"
            android:padding="14dp"
            android:scaleType="fitCenter"
            android:src="@drawable/widget_express_empty_box"
            android:tint="?attr/colorPrimary"
            tools:ignore="UseAppTint" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="26dp"
            android:layout_toEndOf="@id/widget_express_empty_box"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:text="@string/widget_empty_wide"
            android:textColor="@color/widget_empty_text"
            android:textSize="14dp"
            tools:ignore="SpUsage" />
    </RelativeLayout>
</FrameLayout>
