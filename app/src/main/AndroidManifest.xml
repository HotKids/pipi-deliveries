<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".DeliveriesApp"
        android:allowBackup="false"
        android:icon="@mipmap/aicy_ic_launcher"
        android:label="@string/assistant_app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="me.pipi.deliveries.feature.express.ExpressListActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="me.pipi.deliveries.feature.express.ExpressLoginActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name="me.pipi.deliveries.feature.express.ExpressManagerActivity"
            android:exported="false" />
        <activity
            android:name="me.pipi.deliveries.feature.express.ExpressDetailActivity"
            android:exported="false" />

        <receiver
            android:name=".background.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.Express2x2WidgetProvider"
            android:exported="true"
            android:label="@string/card_settings_express_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/express_2x2_appwidget" />
        </receiver>
        <receiver
            android:name=".widget.Express4x2WidgetProvider"
            android:exported="true"
            android:label="@string/card_settings_express_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/express_4x2_appwidget" />
        </receiver>
    </application>
</manifest>
